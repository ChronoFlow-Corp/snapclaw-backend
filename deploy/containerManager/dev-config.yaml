postgres:
  url: "postgresql://containermanager:containermanager@containermanager-db:5432/containermanager?sslmode=disable"

http:
  addr: ":8080"
  read_timeout: 5
  write_timeout: 10

migrations:
  auto: true
  path: "/app/migrations"

image:
  base_path: "/claw-configs"
  build_context:
    - "/workspace/containerManager/images/openclaw"
